const { db } = require('./index.js');


* `GET /likes`
  * `Get all likes`

* `GET /likes/books`
  * `Get book likes`

* `GET /likes/memes`
  * `Get meme likes`

* `GET /likes/vid`
  * `Get vid likes`

* `GET /likes/:id`
  * `Get likes by user`

* `POST /likes`
  * `Post a like`
  
* `DELETE /likes/:id`
  * `Delete a like by id`
